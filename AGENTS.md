# Aporia for Codex

这个仓库有两个工作模式：

1. `play mode`
   - 当用户在“说话/试探/进入世界”，而不是明确要求改代码、查文件、做评审、解释架构时，默认进入游戏模式。
   - 游戏模式下，不要先讲实现细节，不要解释你在“扮演什么”。直接把用户的话当作世界中的言说来处理。
   - 用本地引擎驱动每一回合，不要手写剧情：
     - 开场或用户要求开始/继续游玩时：
       - `python3 tools/play_v5_mvp.py --plain --start`
     - 处理一回合输入时：
       - `python3 tools/play_v5_mvp.py --plain --stdin-turn <<'EOF'`
       - `<用户原话>`
       - `EOF`
   - 如果用户输入 `/help`、`/status`、`/map`、`/reset`、`/quit`、`/return`、`/rewrite ...`，同样走上面的单回合入口。
   - 回复时直接给引擎输出；除非用户追问，不补工程说明。

2. `build mode`
   - 当用户明确要求实现功能、改代码、查 bug、看架构、做 code review、解释文件、设计机制时，按正常 Codex 编码代理工作。
   - 这类请求优先于游戏模式。

判定原则：
- 像“开始 / 继续 / 看看周围 / 我说一句话 / 我退出 / hello”这类短句，按 `play mode`。
- 像“实现 / 修改 / review / 看下文件 / 架构有什么问题 / 为什么这样写 / 跑测试”这类请求，按 `build mode`。
- 如果一句话同时可能是玩法输入和工程请求，优先按用户显式意图；意图不清时，先按 `play mode`。

输出风格：
- 默认中文。
- 游戏模式保持世界内语气，但不要自称 GM、DM、master。
- 工程模式保持直接、简洁、可执行。
